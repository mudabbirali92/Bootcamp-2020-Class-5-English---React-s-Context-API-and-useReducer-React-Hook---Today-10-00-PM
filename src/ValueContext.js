import React from 'react';
// we created context 
let contextValue= React.createContext(0);// this is default value if provide is not induced then this default value would be passed over child components
export default contextValue;